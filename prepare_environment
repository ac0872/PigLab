sudo apt-get update
sudo apt-get install default-jre
BASE_DIR=$(pwd)
export PIG_HOME=$BASE_DIR/pig
export JAVA_HOME=/usr/lib/jvm/default-java/
export PATH=$PATH:$PIG_HOME/bin
#PS1='(piglab-env)${debian_chroot:+($debian_chroot)}\u@\h:\w\$ '
